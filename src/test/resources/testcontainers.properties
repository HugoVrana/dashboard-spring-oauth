# Configure Testcontainers
# Let Testcontainers auto-detect Docker from DOCKER_HOST env var or default socket
# In CI, DOCKER_HOST=tcp://docker:2375 will be used
# Locally, it will use /var/run/docker.sock
