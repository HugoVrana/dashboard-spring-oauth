# JWT configuration for tests (uppercase to match main application)
JWT.SECRET=dGVzdC1zZWNyZXQta2V5LWZvci1pbnRlZ3JhdGlvbi10ZXN0cy1taW4tMjU2LWJpdHM=
JWT.EXPIRATION=86400000

# OAuth2 secret for tests
spring.security.oauth2.secret=test-service-secret

# Email configuration for tests
email.baseUrl=http://localhost:3000
email.fromAddress=Test <test@example.com>
email.verificationTokenExpirationMs=86400000
email.passwordResetTokenExpirationMs=3600000

# Resend API key (mock/test value)
resend.apiKey=re_test_key

# R2 configuration (test values)
r2.accessKeyId=test-access-key
r2.secretAccessKey=test-secret-key
r2.accountId=test-account-id
r2.bucketName=test-bucket

# Grafana configuration (test values)
grafana.apiKey=test-api-key
grafana.url=http://localhost:3100/loki/api/v1/push
